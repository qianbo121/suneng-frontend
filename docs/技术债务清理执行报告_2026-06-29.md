# 官网项目技术债务清理执行报告（2026-06-29）

## 一、执行结论

本轮处理围绕《技术债务审计_2026-06-29》中可直接落地的代码债务展开，已完成三类工作：

1. 删除确认未被当前官网业务继续使用的代码、模块和旧文档。
2. 收敛重复的 SEO、GEO 页面区块、分页、拦截节流等公共逻辑。
3. 补充关键回归测试，并完成前端、后端、后台管理端的 lint、test、build 验证。

当前状态：

- 第一阶段已提交：`b72d859 chore: prune unused website code`
- 第二阶段已提交：`332160d refactor: dedupe site utilities and contact copy`
- 第三到第五阶段代码已完成，尚未提交。
- 未执行 push，未执行部署。

## 二、第一阶段：删除未使用官网代码

提交：`b72d859 chore: prune unused website code`

主要改动：

- 清理前端未使用组件与工具：
  - 删除 `frontend/src/components/furnaces/shared/HeatGradient.tsx`
  - 删除 `frontend/src/components/furnaces/shared/tokens.ts`
  - 删除 `frontend/src/hooks/use-locale.ts`
  - 删除旧的 `frontend/src/lib/seo/keyword-data.ts`
- 收敛产品详情、产品静态数据、证书常量、产品分类等静态代码。
- 调整 `frontend/src/lib/seo/config.ts`、`frontend/src/lib/seo/index.ts`、`frontend/src/lib/seo/keywords.ts`，减少废弃 SEO 分支。
- 调整后台管理端内容服务和类型定义：
  - `admin/src/services/content.ts`
  - `admin/src/types/content.ts`
- 调整后端 `about`、`product` 服务，移除不再对应当前业务入口的冗余读取逻辑。

目的：

- 降低项目中“看起来还在、实际没人用”的代码量。
- 减少后续维护时误判调用关系的成本。

## 三、第二阶段：复用站点工具与联系表单逻辑

提交：`332160d refactor: dedupe site utilities and contact copy`

主要改动：

- 新增后端公共反垃圾/节流工具：
  - `backend/src/common/utils/spam-throttle.ts`
  - `backend/src/common/utils/spam-throttle.spec.ts`
- 让 `contact-message` 和 `custom-requirement` 两条提交入口复用同一套节流判断逻辑。
- 收敛多个页面中的联系入口、CTA 文案和公共工具调用。
- 受影响的前端页面包括：
  - 联系页
  - 服务页
  - 产品详情页
  - 热处理炉厂家 / 江苏工业炉厂家 / 连续热处理线等 GEO 页面
  - 工业炉报价参数、老旧热处理炉大修还是买新等文章页
  - 青山 1250 改造案例页
- 调整 `frontend/src/lib/about.ts`、`frontend/src/lib/news.ts`、`frontend/src/lib/strength.ts`、`frontend/src/lib/seo/jsonld.ts` 等公共数据/结构化数据辅助逻辑。

目的：

- 避免联系表单和需求提交入口各自维护相似的防刷逻辑。
- 减少页面内重复文案和重复辅助函数。

## 四、第三到第五阶段：当前待提交修改

### 1. 后端废弃 CMS 模块删除

已从 Nest 应用入口和源码中移除以下模块：

- `certificate`
- `company-info`
- `contact-message`
- `delivery`
- `sales-outlet`
- `seo`
- `service`
- `strength-category`
- `strength-item`

同步修改：

- `backend/src/app.module.ts`
  - 移除上述模块的注册。
- `backend/prisma/seed.ts`
  - 删除与上述废弃 CMS 模块相关的 seed 写入。
- `backend/src/modules/dashboard/dashboard.module.ts`
  - 改为依赖 `CustomRequirementModule`。
- `backend/src/modules/dashboard/dashboard.service.ts`
  - 移除假的 `unreadContactCount` 和 `todayVisitCount: 0`。
  - 改为返回真实的 `pendingRequirementCount`。
- `backend/src/modules/custom-requirement/custom-requirement.module.ts`
  - 导出 `CustomRequirementService`。
- `backend/src/modules/custom-requirement/custom-requirement.service.ts`
  - 新增 `getPendingCount()`，供 dashboard 查询待处理需求数。

说明：

- 本轮没有删除 Prisma schema 中的模型，也没有生成数据库 migration。
- 因此当前风险主要在代码 API 层，不涉及直接改库表。
- 如果外部还有系统调用这些被删模块的接口，会产生兼容性影响；发布前应确认没有外部依赖这些废弃接口。

### 2. SEO 元数据生成逻辑收敛

主要改动：

- `frontend/src/lib/seo/index.ts`
  - 删除旧的 `buildSeoMetadata`、`buildLocalizedUrl`、`getSiteUrl`、Open Graph locale 辅助逻辑。
  - 保留仍在使用的图片 alt 辅助逻辑。
- `frontend/src/lib/seo/metadata.ts`
  - `keywords` 支持 `string | string[]`，兼容迁移中的详情页和列表页。
- `frontend/src/lib/news.ts`
- `frontend/src/lib/about.ts`
- `frontend/src/lib/strength.ts`
- `frontend/src/app/[locale]/partner/page.tsx`
  - 迁移到统一的 `buildMetadata`。
- `frontend/src/lib/seo/jsonld.ts`
  - 新增 `getWebPageJsonLd()`。
- 新增测试：
  - `frontend/src/lib/seo/metadata.spec.ts`

测试覆盖点：

- 新闻列表 metadata 的 canonical、languages、Open Graph、Twitter。
- 实力页面 metadata 的关键词和 locale。
- 关于页面 metadata 的多语言 canonical。
- `keywords` 字符串兼容。
- 新闻详情 canonical URL helper。

### 3. GEO 页面公共区块抽取

新增公共组件文件：

- `frontend/src/components/geo-pages/GeoPageBlocks.tsx`

组件包括：

- `GeoSection`
- `GeoHeroTags`
- `GeoBulletList`
- `GeoFactList`
- `GeoFaqGrid`
- `GeoContactCta`

已接入页面：

- `frontend/src/app/[locale]/solutions/rechuli-lu-changjia/page.tsx`
- `frontend/src/app/[locale]/solutions/jiangsu-gongye-lu-changjia/page.tsx`
- `frontend/src/app/[locale]/solutions/continuous-heat-treatment-line/page.tsx`
- `frontend/src/app/[locale]/articles/laojiu-rechuli-lu-daxiu-haishi-maixin/page.tsx`
- `frontend/src/app/[locale]/articles/gongye-lu-baojia-canshu/page.tsx`
- `frontend/src/app/[locale]/service/furnace-renovation-overhaul/page.tsx`
- `frontend/src/app/[locale]/case/anonymous-tsingshan-1250-renovation/page.tsx`

说明：

- 原本计划放在 `components/GEO` 或 `components/geo`，但项目 `.gitignore` 会忽略 `GEO/` 目录。
- 为避免新增文件被 Git 忽略，最终使用 `geo-pages` 目录名。

### 4. 新闻分页复用

主要改动：

- `frontend/src/components/ui/Pagination.tsx`
  - 在原有 `onPageChange` 客户端模式基础上，新增链接模式：
    - `baseHref`
    - `pageParam`
- `frontend/src/components/news/NewsListCards.tsx`
  - 删除手写 `NewsPagination`。
  - 改为使用共享 `Pagination`。

目的：

- 避免列表页各自维护分页 UI。
- 保留已有客户端分页能力，同时支持新闻列表这种 URL 查询参数分页。

### 5. 历史技术债文档归档

已将根 `docs/` 下旧技术债文档移入：

- `docs/archive/技术债务修复_执行版v2.md`
- `docs/archive/技术债务修复方案.md`
- `docs/archive/技术债务重构报告.md`

目的：

- 保留历史资料。
- 避免旧方案和当前执行状态混在同一层目录里。

## 五、明确未做的事项

审计项 #31：将 20+ 个 client component 内联中英文文案迁移到 `next-intl` messages。

本轮没有执行该项，原因：

- 实际扫描后发现范围不止 20 处，涉及产品详情、首页、布局、关于、新闻等多处核心页面。
- 当前 `frontend/src/messages/zh.json` 和 `frontend/src/messages/en.json` 规模很小，直接迁移会变成一次全站 i18n 信息架构调整。
- 审计中该项本身优先级较低，且建议“先补测试 / 先不动”。
- 若强行混入本轮，会显著扩大回归面，不适合作为这次清理提交的一部分。

建议后续单独开一轮处理 #31，先定消息命名规范和页面迁移顺序，再逐页替换。

## 六、验证结果

本轮代码修改后已执行并通过：

- `pnpm --dir frontend lint`
- `pnpm --dir frontend test`
  - 6 个测试文件通过
  - 31 个测试通过
- `pnpm --dir frontend build`
- `pnpm --dir backend lint`
- `pnpm --dir backend test`
  - 7 个测试套件通过
  - 22 个测试通过
- `pnpm --dir backend build`
- `pnpm --dir admin lint`
- `pnpm --dir admin test`
  - 1 个测试文件通过
  - 3 个测试通过
- `pnpm --dir admin build`
- `git diff --check`

结论：

- TypeScript 构建通过。
- 单元测试通过。
- lint 通过。
- 当前 diff 没有空白字符错误。

## 七、发布前风险点

1. 废弃后端模块删除是 API 兼容性变化。
   - 如果后台菜单、外部脚本或第三方系统仍调用这些接口，需要先同步删除或替换调用方。

2. 本轮未改数据库 schema。
   - 数据库中可能仍保留旧表或旧数据。
   - 这降低了本轮发布风险，但也意味着“数据库层清理”尚未发生。

3. GEO 页面复用组件后视觉应保持一致。
   - 当前已通过构建和测试。
   - 若发布前有时间，建议再跑一次关键页面截图检查。

4. `技术债务审计_2026-06-29.md` 是审计文档提取副本，目前仍是未跟踪文件。
   - 它不是本轮代码变更的必要文件。
   - 提交时建议不要误加入，除非决定把审计原文也纳入仓库。

## 八、建议提交范围

第三到第五阶段建议作为一个提交：

建议提交信息：

```text
refactor: remove legacy CMS modules and dedupe SEO/GEO pages
```

建议包含：

- 后端废弃模块删除
- dashboard pending requirement 指标调整
- seed 清理
- SEO metadata 统一迁移和测试
- GEO 公共区块抽取
- 新闻分页复用
- 旧技术债文档归档
- 本执行报告

建议排除：

- 根目录未跟踪的 `技术债务审计_2026-06-29.md`，除非明确要把审计原文入库。
