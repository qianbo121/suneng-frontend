# 官网 GEO P0-0 白名单审计

日期：2026-07-29
范围：仅 `jssngyl.cn` 官网 P0 真实性修复；智能工厂、产品追溯、视觉回归与其他工作流不纳入。

## 现场保护

- 原工作树：`/Users/qianbo/Desktop/Coding/官网+GEO`
- 隔离工作树：`/Users/qianbo/Desktop/Coding/官网-GEO-P0`
- 实施分支：`geo/p0-p1-p2-20260729`
- 生产目录：`/opt/website`
- 生产服务器白名单文件与隔离工作树修改前基线逐项哈希一致。
- 原工作树与生产目录均存在大量其他改动；禁止全量覆盖、全量暂存或整仓回滚。

## P0 提交白名单

| 文件 | 目的 |
|---|---|
| `frontend/src/app/[locale]/page.tsx` | 停止把旧 Banner/Partner 数据作为 RSC 客户端属性序列化 |
| `frontend/src/components/home/HeroBanner.tsx` | 首页首屏改用经过确认的静态中英文实体文案 |
| 两个采购文章 `page.tsx` | 增加 Article JSON-LD 与真实发布日期 |
| `frontend/src/app/sitemap.ts` | 仅在存在真实日期时输出 `lastmod` |
| `frontend/src/components/about/AboutZhContent.tsx` | 删除重复 Organization 实体 |
| `frontend/src/i18n/routing.ts`、`frontend/src/middleware.ts` | 统一 HTML hreflang、移除语言 Cookie、设置公共页共享缓存 |
| `frontend/src/mock/siteSettings.ts` | WhatsApp 与真实销售号码对齐 |
| 四个 `*.spec.ts` | 固化 sitemap、路由、JSON-LD 与联系方式规则 |
| 本审计文档 | 留存白名单和排除项 |

## 明确排除

- `frontend/src/app/p/`、`frontend/public/trace/`、`trace-preview/`、`components/traceability/`
- Playwright 视觉回归配置、基线图与其依赖变更
- `.env.production.example`、`docker-compose.prod.yml`、`nginx.prod.conf.template`
- 根/前端 `package.json`、`pnpm-lock.yaml`
- 与本次 P0 无关的技术债务文档、策略草稿和其他本地修改
- 智能工厂任何代码与部署

## 发布门槛

1. 前端测试、lint、build 与 `git diff --check` 全通过。
2. 只暂存上述白名单；提交前复核 staged 文件清单。
3. 生产端逐文件备份并核对修改前哈希。
4. 仅同步白名单运行文件；不执行整仓覆盖。
5. 部署后验证官网、后台、新闻接口、智能工厂入口与容器状态。
6. 验证文章 Article JSON-LD、真实日期、sitemap lastmod、首页源码污染和日志继续写入。
