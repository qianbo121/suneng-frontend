# 剩余债务接入结果 · 2026-09-20

本批把旧修复分支中未采用的代码接入已上线主线基线，形成独立可审查版本。没有整体覆盖旧分支；当前英文搜索修复、8 页公开清单、旧网址处理和管理后台缓存保护均保留。此记录只证明本地接入与检查，不代表推送、合并或正式上线。

## 可以通过

- 登录地址变体共用限速；正常后台查询不占登录额度。另修正旧方案的重载兼容缺口，使用新共享区域名称，避免改变旧计数键导致代理拒绝重载。
- 后端发布先核对获准的变更和备份，再应用增量变更，随后检查新查询并切换。备份过期、损坏或未通过时停止；最终查询未真正完成时拒绝通过。原前台、后台单独发布与失败回退保护保留。
- 备份告警校验平台是否接受消息；每日备份方式和保留周期不变，未接入新群或外部存储。
- 网站地图过滤非法和未来日期，不将中文修订时间套给英文。拆分中英文产品资料、工业炉资料和井式炉资料，历史选型代码移到测试目录，生产状态类型独立。

## 已测试与边界

本轮 274 项测试通过：前端针对性测试 180 项、备份告警 21 项、备份校验 4 项、后端更新顺序 5 项、发布保护 64 项。类型检查、修改文件代码规范检查、选型公开边界检查及差异检查通过。

中英文产品资料、7 种工业炉资料、井式炉 12 组资料均与主线完整序列化结果一致，分别为 197598、200841、81808、8777 字节；公开导出和查找行为保留，井式炉页面渲染函数逐字不变。没有修改样式、图片或正文，也没有把数据一致性当成全站浏览器验收。

本机 Nginx 1.27.5 的 29 次 POST 请求通过，验证旧规则到新规则平滑重载且主进程不变、地址变体共享限速、普通查询不受限以及相邻撤下地址保持关闭。测试只包含抽取的真实限速规则和本机假后端，没有向生产登录发送请求。完整模板的本机预检两次受 macOS 内存页与 Linux 缓存路径差异阻断，未记通过；正式发布前仍须核对服务器完整配置、证书与其他站点规则。

首轮前端检查曾从错误工作目录启动，产生 15 项找不到素材或内容的失败；切回项目规定的 frontend 目录后，180 项通过，未改测试断言。新增发布测试首轮夹具漏掉受保护服务的条件判断，修正后 64 项通过。失败记录不充作通过。

## 仍需分开完成

本批基于 `dbcdbdb7709576c7d70e68269693f7a197369175`，独立目录为 `独立站-worktrees/debt-integration-20260920`。正式发布需要：推送并通过主线检查、生成前台候选、候选验收、安装同版本代理模板与备份/发布工具、完整配置预检和发布后检查。本批不需要替换当前后端或管理后台镜像，也不执行数据库变更；后端发布顺序修复供未来明确包含后端的发布使用。工具安装须保留 `backup_gate.py` 与 `check-backend.cjs` 的同目录关系。

原开发目录 470 项已跟踪差异已有分类与保存补丁。本轮补列 1075 个未跟踪文件或目录条目：历史资料与验收产物 910 项、代码/工具候选 91 项、素材/内容候选 33 项、其他本机资料 41 项。逐项路径和保留动作在本机证据目录；没有删除、搬走或上传这些资料，也没有声称逐项内容价值已审完。其他任务仍在使用该目录，不能为制造干净状态清空它。

自动异地备份、外部监测、新告警群维持暂缓。搜索平台验证值与百度提交安全入口仍待对应账号确认，不编造验证值、不关闭证书检查、不降级传输提交密钥；已有失败队列继续保留。

本机证据位于 `/Users/qianbo/Desktop/Coding/独立站-release-evidence/20260920-debt-integration/`：`checks-summary.json`、`content-equivalence.json`、`release-tests.log`、`login-reload-packaged.json`、`static-checks.json`、`legacy-directory-inventory.json`。源码提交状态以 Git 记录为准，推送与上线须另有回执。
