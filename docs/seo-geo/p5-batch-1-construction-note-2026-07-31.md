# P5 权威主题页第一批施工说明

日期：2026-07-31
范围：仅 B「热处理炉温度不均整改与验收」与 F「改造风险、周期与生产影响」
状态：本地施工与验收；未提交、未部署、未做索引提交

## 一、施工前审计结论

1. `docs/seo-geo/p5-authority-topic-drafts-2026-07-30.md` 给出了 B、F 的内容骨架，但没有写明 slug。为避免新造命名，本批沿用已形成的中文语义 slug，并按要求放在 `/zh/solutions/`：
   - `/zh/solutions/rechuli-lu-wendu-bujun-zhenggai`
   - `/zh/solutions/rechuli-lu-gaizao-fengxian-zhouqi`
2. 事实台账核验结果：
   - `SN-CASE-P1-013`：技术准确为“资料确认”，公司允许为“通过”；
   - `SN-CASE-P1-014`：技术准确为“资料确认”，公司允许为“通过”；
   - `SN-CASE-P0-008-F01~F06`：技术准确均为“待唐工复核”，本批未引用、未跨页复用。
3. 模板与生产样式均保留 `html { scroll-padding-top: 88px; }`，页面结构严格按：
   直接答案 → 判断信号 → 对比/决策表 → 验收证据 → FAQ → 咨询参数清单。

## 二、页面数字与事实编号

### B 温度不均整改与验收

| 页面数字或参数 | 事实编号 | 本批处理 |
| --- | --- | --- |
| 项目炉膛、温度、装炉量、温度均匀性设计要求 | `SN-CASE-P1-013` | 未展示具体数字。页面只公开事实编号及“不把项目设计要求作为通用承诺”的边界。 |
| 空炉/负载、测点数量、保温时间、仪器与校准、标准号 | 无可跨项目放行的统一数值 | 全部改为定性条件；明确写“按项目技术协议和适用标准单独确认”。 |
| UI 节号 `01` 至 `06` | 不属于工程事实 | 仅为已审核模板的阅读顺序，不参与技术结论。 |
| 发布与修改日期 | `page-data.ts` | 分别使用真实页面创建时间与本次内容修改时间，不使用共享硬编码日期。 |

### F 改造风险、周期与生产影响

| 页面数字或参数 | 事实编号 | 本批处理 |
| --- | --- | --- |
| 炉体尺寸、温度、温控区、功率、助燃空气设计目标 | `SN-CASE-P1-014` | 未展示具体数字。页面只公开事实编号及“项目配置、设计目标不外推为通用工期或效果承诺”的边界。 |
| 停产天数、总周期、节能比例、固定总价 | 无可跨项目放行的统一数值 | 不给固定数字；按诊断设计、采购预制、停产施工、调试验收分阶段定性说明，并写“按项目单独确认”。 |
| UI 节号 `01` 至 `06` | 不属于工程事实 | 仅为已审核模板的阅读顺序，不参与技术结论。 |
| 发布与修改日期 | `page-data.ts` | 分别使用真实页面创建时间与本次内容修改时间，不使用共享硬编码日期。 |

## 三、因无已放行事实而采用的定性表达

### B 页面

- 不提供通用温度均匀性数值；
- 不提供通用测点数量、保温时间或标准号；
- 不将空炉结果外推到负载生产；
- 不将 `SN-CASE-P1-013` 的设计要求写成项目实测或通用验收结果；
- 对测试状态、测点、仪器、校准和适用标准统一写为按项目确认。

### F 页面

- 不提供固定停产天数或固定总周期；
- 不提供固定节能比例、产能结果或无条件总价；
- 不将 `SN-CASE-P1-014` 的配置、功率或设计目标写成其他项目的通用结果；
- 对隐蔽工程、交叉施工、切换回退和负载验收采用条件式表述；
- 在范围、接口和验收条件未确认时，只提供阶段判断。

## 四、结构化数据与内链

- 两页均输出 `Article`、`BreadcrumbList`、复用 `/#organization` 的组织实体、`FAQPage`；
- 未使用 `Dataset`；
- 改造服务页、报价参数页、修还是换页均各自建立了指向两张新页的正文内链；
- 两张新页均反向链接到上述三个既有页面；
- 两页已加入 sitemap，并使用各自的真实 `modifiedTime`。

## 五、约束执行

- 未修改青山案例页；
- 未修改 `outputs/`；
- 未触碰智能工厂、Nginx、Docker Compose；
- 未提交、未部署、未做索引提交。
