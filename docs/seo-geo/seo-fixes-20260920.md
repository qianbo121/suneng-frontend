# 搜索问题修复交接 — 2026-09-20

本文件记录本地修复与交接证据，正常页面外观和正文不改。随后网站负责人已授权提交并一次完成上线；是否实际上线以正式发布回执和上线后核查为准，不以此文档推断。

## 可以通过

在独立目录 `/Users/qianbo/Desktop/Coding/独立站-worktrees/seo-fixes-20260920` 的 `codex/seo-fixes-20260920` 分支修改，以当时已核对的线上主线 `b728bed8fef6f4688a9205817d741a52e651a187` 为基线。本任务未编辑原开发目录的业务文件。

新闻和产品详情的带点地址进入既有存在性检查，不再仅因后缀像文件就绕过。案例页添加空搜索参数也不能绕过总页数边界；第一页无结果仍允许显示。未知 PHP 地址停止统一跳首页，精确旧地址映射保留。

搜索提交任务从线上网站地图发现新增网址及真实更新时间变化，不依赖候选构建完成。初始基线是实读的 208 个线上网址，仅补已确认上线的 8 篇指南。待提交项、线上快照一起保存，失败保留、成功去重。

两个正式构建流程接通四个平台的所有权验证值。英文搜索文案另存：为现有本地英文登记题目及 4 篇近期后台英文新闻准备短标题；首轮实读的 69 篇线上新闻题目均有对应短版。新闻列表及河南案例搜索标题亦缩短，案例描述保留“方案参数不等于实测/验收结果”的限定。页面大标题、正文、文章结构化信息使用原文；日期策略未改。

收尾补充：短版文案只覆盖当时核对过的旧搜索文案；后台后来单独改搜索标题或摘要时，新文案优先，即使页面大标题没有改。河南案例摘要也有同样的更新保护。

## 已测试与仍存在的问题

收尾最终相关测试：9 个文件、133 项通过；提交队列及网址选择测试 19 项通过，共 152 项。新增检查覆盖后台新文案优先与完整新闻请求判断链路。相关代码规范检查、类型检查、差异空白检查通过。

本地案例电脑 1440×1000、手机 390×844 实际渲染通过，没有横向溢出，手机菜单可打开。浏览器标签显示 47 字符短标题，页面保留完整原题；新闻及案例可见组件与基线逐字一致，没有修改样式文件。本地正常案例返回成功，错误产品地址和空查询参数下的越界案例返回真正不存在。

截图：[电脑](/private/tmp/suneng-seo-fixes-20260920.ViUQvP/case-desktop.png)、[手机](/private/tmp/suneng-seo-fixes-20260920.ViUQvP/case-mobile.png)。

增量队列对真实线上地图只读演练：208 个网址，两平台各计算出 8 个待提交项；磁盘上的测试队列仍为空，未执行提交请求。

收尾完成了真实本地服务的 12 项请求检查：9 个错误地址返回真正不存在并禁止收录；3 个正常新闻地址返回成功并保留原题正文，其中包含合法带点文章地址。检查经过真实页面框架与请求拦截层，但内容来自仅限本机的测试服务，不能当作正式候选镜像或线上验收。测试服务未收到任何写入请求。

本地请求检查产生的两处开发配置变化已恢复，临时检查进程已结束；未停止原开发服务。临时检查脚本与生成文件不进入交付清单。

收尾只读核对了仓库变量与受保护配置的名称，两处均未找到四个平台的站长验证配置；没有读取或展示配置内容，也没有登录搜索后台取值或证明验证状态。缺少网页标记不等于其他验证方式无效。未运行完整构建、全站回归、服务器规则的运行时语法检查或任何上线步骤。本地不具备服务器规则运行环境，因此该部分目前只有配置规则测试。

## 建议怎么处理

已获得提交与发布授权，按固定程序包发布流程推进；上述本地通过不等于线上修复。发布前重新核对已发布主线，避免覆盖期间其他任务的新改动。若需要改正常页面界面，先截图交由负责人确认。

未知 PHP 地址属于服务器转发规则，单独切换前端镜像不会使其生效。需在获准更新相应网站配置后，先检查服务器规则语法，再核对实际响应；其他服务与精确旧地址映射不动。

四个验证值从百度、必应、360、搜狗后台取得后再填入正式构建配置，不填占位符。已有页面修改但没有真实修改时间时，发布核查后仍需在提交任务中明确填入这些网址；不要制造网站地图日期。详见 [队列运行说明](./search-submission-queue.md)。
