# 官网管理员 seed 拆弹阶段报告

日期：2026-08-07

阶段结论：代码拆弹与本地验证完成，具备整阶段复核条件；未部署生产、未轮换 JWT、未修改生产数据库或管理员账号。

## 1. 事实基线

- 权威基线为生产同版提交 `f925289315012bbc2bee04499f3ee5d64f669dee`。
- 生产后端镜像的 OCI revision 与该提交一致。
- 经授权在生产容器内只读比对：`admin` 存在且不匹配已知默认密码；`editor` 不存在。
- 比对没有调用登录接口、没有输出密码哈希、没有执行数据库写入。
- 原内容 seed 曾在每次执行时重置管理员密码、角色和启用状态，并创建另一个默认账号。

## 2. 本阶段修改

### 内容 seed

- 删除管理员账号创建和更新函数。
- 删除 `AdminRole` 和 bcrypt 依赖。
- 删除管理员 seed 调用。
- seed 完成日志明确声明管理员账号没有被创建或修改。
- 保留生产 destructive-seed 的 fail-closed 门禁。

### 活动文档与 Swagger 示例

- 数据库初始化文档明确：内容 seed 只处理内容数据，永远不管理管理员账号。
- 生产部署默认只执行迁移，不执行 seed。
- 首个超级管理员必须通过独立、受控的一次性流程创建。
- 上线清单不再把“默认密码首次修改”视为安全措施。
- 登录、修改密码和创建管理员 DTO 不再展示公开默认口令。

### 防回归门禁

标准后端 Jest 新增 `content seed administrator safety contract`：

1. 扫描 `backend/prisma/` 下全部可执行 seed 源文件，不得引用管理员模型、角色、密码哈希或 bcrypt，也不得通过大小写变化绕过；
2. `prisma.config.ts` 必须继续指向已经复核的 `prisma/seed.ts` 入口；
3. 生产 destructive-seed 门禁必须保持 fail-closed；
4. 全仓活动源码、配置和文档不得重新出现已知默认管理员凭据。

由于内容 seed 对管理员模型不存在任何代码路径，重复执行内容 seed 不能修改管理员密码、角色或启用状态。未来若把管理员写入路径加回 seed，标准测试会直接失败。

## 3. 验证结果

- 后端 Jest：11 个测试套件、36 项测试全部通过。
- ESLint：通过。
- TypeScript `tsc --noEmit`：通过。
- Nest 后端构建：通过。
- Prettier（本阶段新增和修改的活动源码、DTO、文档）：通过。
- `git diff --check`：通过。
- 全仓活动文件扫描：已知默认管理员口令命中 0。
- 内容 seed 扫描：管理员模型、角色、bcrypt、管理员 seed 路径命中 0。

本机 Node 为 24.15.0，仓库声明的目标版本是 Node 22.23.1；安装时出现 engine warning，但测试、类型检查和构建均通过。生产 Docker 镜像仍按仓库 Dockerfile 的 Node 22 环境构建，本阶段没有改变运行时版本。

额外尝试按 `backend/Dockerfile` 构建 Node 22.23.1 验证镜像，但 Docker 容器访问项目固定的 `registry.npmmirror.com` 超时；独立容器网络探针同样在 15 秒内无响应。该项属于当前 Docker 网络/镜像源阻断，没有出现应用编译错误。本阶段未为绕过验证而修改 Dockerfile 或依赖源。

## 4. 明确未做

- 未执行生产 seed。
- 未修改生产管理员密码、角色或启用状态。
- 未轮换生产 JWT 密钥。
- 未部署官网生产环境。
- 未建设数炬专用只读接口。
- 未启用数炬 `shadow`；数炬继续保持 `disabled`。

## 5. 后续顺序

1. Claude 独立复核本提交及上述门禁；
2. 复核通过后，再安排生产 JWT 密钥轮换窗口；
3. 随后建设独立密钥、强制内网、官网侧调用日志的数炬新闻只读接口；
4. 只读接口完成并通过负向写入测试前，数炬不得启用生产影子模式。
