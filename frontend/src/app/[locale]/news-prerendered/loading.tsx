// Plain resource URLs are rewritten into this sibling route, so they do not
// inherit news/loading.tsx. Keep the same visible loading state for both routes.
export { default } from '../news/loading';
